import { Container, Typography } from '@mui/material';

const contact = () => {
	return (
		<Container>
			<Typography variant='h1'>It's Contact Page</Typography>
		</Container>
	);
};

export default contact;
