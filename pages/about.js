import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

export default function About() {
	return (
		<Container>
			<Typography variant='h1'>It's About Page</Typography>
		</Container>
	);
}
